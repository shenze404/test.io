/* 基本页面样式 */
body {
    background: linear-gradient(to bottom, #00000a, #062053); /* 上黑下蓝的渐变 */
    background-size: cover; /* 背景图像自适应页面大小 */
    background-position: center; /* 背景图像居中 */
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
}

/* 顶部导航栏样式 */
header {
    background-color: rgba(0, 0, 0, 0.7); /* 半透明的黑色背景 */
    color: white;
    padding: 20px;
    text-align: center;
    position: fixed; /* 固定在页面顶部 */
    width: 100%; /* 使导航栏占据全宽 */
    top: 0;
    left: 0;
    z-index: 10; /* 确保它在页面其他内容之上 */
}

header nav ul {
    list-style-type: none;
    padding: 0;
    margin: 0;
}

header nav ul li {
    display: inline;
    margin: 0 20px;
}

header nav ul li a {
    color: white;
    text-decoration: none;
    font-size: 18px;
    transition: color 0.3s ease;
}

header nav ul li a:hover {
    color: #ff7e5f; /* 鼠标悬停时改变文字颜色 */
}

/* 主内容区样式 */
main {
    padding: 100px 20px 20px 20px; /* 上面有导航栏，留出一些空白 */
    text-align: center;
}

#intro {
    background-color: rgba(255, 255, 255, 0.8); /* 半透明白色背景 */
    border-radius: 10px;
    padding: 20px;
    margin: 20px auto;
    max-width: 600px; /* 最大宽度，避免太大 */
}

h1 {
    font-size: 36px;
    font-weight: bold;
}

h2 {
    font-size: 28px;
    color: #333;
}

/* 底部样式 */
footer {
    text-align: center;
    padding: 20px;
    background-color: rgba(0, 0, 0, 0.7);
    color: white;
    position: fixed;
    bottom: 0;
    width: 100%;
}
